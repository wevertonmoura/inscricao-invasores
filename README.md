# 🏃‍♂️ Treinão Invasores 2026 - Landing Page

> "Todo dia é dia de invadir seus próprios limites."

Uma Landing Page de alta performance desenvolvida para gerenciar as inscrições do evento de corrida do grupo **INVASORES**. O sistema conta com formulário de cadastro, validação em tempo real, geração de tickets digitais e integração com banco de dados.

![Status do Projeto](https://img.shields.io/badge/Status-Finalizado-green)
![License](https://img.shields.io/badge/License-MIT-blue)

## 📱 Preview do Projeto

*(Se tiver um print da tela, coloque o link aqui ou suba na pasta public e linke assim: /public/print.png)*

## 🚀 Tecnologias Utilizadas

O projeto foi construído utilizando as tecnologias mais modernas do mercado para garantir velocidade e SEO:

* **[Next.js 14](https://nextjs.org/)** - Framework React para produção (App Router).
* **[TypeScript](https://www.typescriptlang.org/)** - Tipagem estática para segurança do código.
* **[Tailwind CSS](https://tailwindcss.com/)** - Estilização utility-first para design responsivo e moderno.
* **[Supabase](https://supabase.com/)** - Backend as a Service (PostgreSQL) para armazenamento das inscrições.
* **[Lucide React](https://lucide.dev/)** - Biblioteca de ícones leves e performáticos.
* **[Canvas Confetti](https://www.npmjs.com/package/canvas-confetti)** - Animações de celebração.

## ✨ Funcionalidades

* **⚡ Inscrição Rápida:** Formulário otimizado com validação de campos (Nome, Email, Distância, Saúde).
* **🎟️ Ticket Digital Dinâmico:** Geração automática de comprovante com número de inscrição (#1001), nome do atleta e marca d'água estilizada.
* **🔒 Segurança (RLS):** Banco de dados protegido com Row Level Security (apenas inserção pública, sem permissão de delete/update).
* **📱 Mobile First:** Design totalmente responsivo, focado na experiência em celulares.
* **💬 Integração WhatsApp:** Redirecionamento automático para o grupo VIP e função "Convidar Amigo".
* **⏳ Countdown:** Contagem regressiva em tempo real para o dia do evento.

## 🛠️ Como Rodar Localmente

Siga os passos abaixo para rodar o projeto na sua máquina:

1.  **Clone o repositório:**
    ```bash
    git clone [https://github.com/SEU-USUARIO/nome-do-repo.git](https://github.com/SEU-USUARIO/nome-do-repo.git)
    ```

2.  **Instale as dependências:**
    ```bash
    npm install
    # ou
    yarn install
    ```

3.  **Configure as Variáveis de Ambiente:**
    Crie um arquivo `.env.local` na raiz do projeto e adicione suas chaves do Supabase:
    ```env
    NEXT_PUBLIC_SUPABASE_URL=sua_url_aqui
    NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anonima_aqui
    ```

4.  **Rode o servidor de desenvolvimento:**
    ```bash
    npm run dev
    ```
    Acesse `http://localhost:3000` no seu navegador.

## 🗄️ Estrutura do Banco de Dados (Supabase)

O projeto utiliza uma tabela única chamada `inscricoes`. Execute o SQL abaixo no SQL Editor do Supabase para criar a estrutura:

```sql
create table inscricoes (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  name text not null,
  email text not null unique,
  level text not null, -- Distância (3km, 5km, 10km)
  health text not null, -- Condição de saúde
  health_details text,
  terms_accepted boolean not null,
  numero_inscricao serial -- Gera o número #1001, #1002 automaticamente
);

-- Políticas de Segurança (RLS)
ALTER TABLE inscricoes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Permitir Inscrição Pública" ON inscricoes FOR INSERT TO anon WITH CHECK (true);
CREATE POLICY "Permitir Leitura Pública (Validação)" ON inscricoes FOR SELECT TO anon USING (true);